---
description: Build and Run Android App in Production mode (Signed Release) on a connected device.
---
This workflow automates the process of building signed production assets and running them on your connected Android device.

Steps:
1. Ensure a device is connected via USB and authorized.
2. Run the automation script.

// turbo
3. Run test-android-prod.ps1.

```powershell
./test-android-prod.ps1
```
