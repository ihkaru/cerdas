#!/bin/sh

# Pre-push hook: Runs strict verification (Lint + Build + Backend Checks)
# Bypass with: git push --no-verify

echo "🛡️  Running strict pre-push verification..."

# Execute verification script
./verify-local.sh

if [ $? -ne 0 ]; then
    echo ""
    echo "❌ PUSH BLOCKED: Local verification failed."
    echo "   Please fix the errors above."
    echo "   To bypass (NOT RECOMMENDED): git push --no-verify"
    exit 1
fi

exit 0
