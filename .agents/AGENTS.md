# Workspace Rules for Antigravity Agent

## Verification & Quality Assurance
- **Always run linting after modifying code**: Whenever source code files in `apps/` or `packages/` are modified, always run `pnpm lint` (or `pnpm --filter <app> lint`) to verify that no linting errors or broken rules were introduced.
